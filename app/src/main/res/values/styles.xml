<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/secondaryLightColor</item>
        <item name="android:textColorSecondary">@color/secondaryLightColor</item>
        <!--<item name="android:homeAsUpIndicator">@color/secondaryLightColor</item>
        <item name="actionMenuTextColor">@color/secondaryLightColor</item>
        <item name="drawerArrowStyle">@color/secondaryLightColor</item>
        <item name="colorControlNormal">@color/secondaryLightColor</item>
        <item name="windowActionBarOverlay">true</item>-->
    </style>

</resources>
